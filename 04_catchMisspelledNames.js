// Catch Misselled Variable and Function Names
/* JavaScript variable and function names are case sensitive.
Misspellings are a common source of reference errors. */

/* Fix the two spelling errors in the code so the 
netWorkingCapital calculation works. */

let receivables = 10;
let payables = 8;
let netWorkingCapital = recievables - payable;
console.log(`Net working capital is: ${netWorkingCapital}`);